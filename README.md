# نظام نقاط البيع والمحاسبة (POS & Accounting System)

برنامج محاسبة ونقاط بيع (POS) شامل مع دعم لتعدد العملات واللغات، مصمم للعمل محليًا على أجهزة سطح المكتب والجوال. تم بناء هذا التطبيق ليكون سريعًا وموثوقًا ويعمل دون اتصال بالإنترنت.

## 🌟 الميزات الرئيسية

- **نقطة بيع سريعة (POS):** واجهة سهلة وسريعة لعمليات البيع اليومية مع بحث بالباركود.
- **إدارة الفواتير:** إنشاء وتعديل فواتير البيع والشراء التفصيلية.
- **إدارة المخزون:** تتبع المنتجات، الفئات، الكميات، وتعدد الأسعار (تكلفة، جملة، بيع).
- **إدارة الذمم:** إدارة حسابات العملاء والموردين وتتبع الأرصدة المدينة والدائنة.
- **كشوفات الحساب:** عرض كشف حساب تفصيلي لأي عميل أو مورد.
- **إدارة المصاريف:** تسجيل وتصنيف جميع النفقات التشغيلية.
- **إدارة الصناديق:** تتبع السيولة النقدية في صناديق وحسابات متعددة، مع دعم للإيداع والسحب والتحويل.
- **تقارير شاملة:** مجموعة من التقارير المالية والإدارية (أرباح وخسائر، حركة الصندوق، جرد المخزون، أفضل المبيعات).
- **إعدادات متقدمة:** إدارة أسعار الصرف، معلومات الشركة، والنسخ الاحتياطي والاستعادة.
- **النسخ الاحتياطي والدمج الذكي:** إمكانية أخذ نسخة احتياطية كاملة، واستيراد البيانات ودمجها بذكاء بين الأجهزة المختلفة دون حذف البيانات الحالية.
- **دعم تعدد العملات:** إجراء المعاملات بأي عملة مع التحويل التلقائي للعملة الأساسية.
- **وضع داكن/فاتح:** واجهة مريحة للعين في مختلف ظروف الإضاءة.
- **يعمل دون اتصال:** جميع البيانات يتم تخزينها محليًا، مما يضمن استمرارية العمل.

## 🛠️ التقنيات المستخدمة

- **الواجهة الأمامية:** React, TypeScript
- **إدارة الحالة:** Zustand
- **التصميم:** Tailwind CSS
- **قاعدة البيانات:** Capacitor/SQLite (يعمل على الويب، أندرويد، و Electron)
- **تغليف سطح المكتب:** Electron

## 🏗️ الهيكل البرمجي

يتبع المشروع هيكلًا برمجيًا منظمًا يعتمد على نمط **المستودع (Repository Pattern)** لفصل منطق العمل عن طريقة الوصول إلى البيانات.

- **`components/`**: يحتوي على مكونات React القابلة لإعادة الاستخدام.
- **`hooks/`**: يحتوي على "Hooks" مخصصة لمنطق متكرر (مثل `useCurrency`, `useReports`).
- **`screens/`**: يمثل كل ملف شاشة رئيسية في التطبيق.
- **`services/repositories/`**: قلب منطق الوصول للبيانات. كل مستودع (مثل `SalesRepository`, `InventoryRepository`) مسؤول عن جزء محدد من البيانات، مما يجعل الكود نظيفًا وسهل الصيانة.
- **`store/`**: لإدارة الحالة العامة باستخدام Zustand.
- **`electron/`**: يحتوي على الملفات اللازمة لتشغيل التطبيق على سطح المكتب باستخدام Electron.

## 🚀 التشغيل والتجهيز للنشر

هذا المشروع مصمم ليكون تطبيق ويب أولاً، ولكن تم بناؤه ليكون جاهزًا للتغليف كـ:
- **تطبيق Android** باستخدام [Capacitor](https://capacitorjs.com/).
- **تطبيق سطح مكتب (Windows/Linux)** باستخدام [Electron](https://www.electronjs.org/).

### 1. تثبيت الاعتماديات

```bash
npm install
```

### 2. التشغيل كـ تطبيق ويب (للتطوير السريع)

```bash
npm run dev
```
سيتم تشغيل التطبيق على `http://localhost:5173`.

> ⚠️ **تنبيه هام:** هذا الأمر مخصص لتطوير واجهة المستخدم في المتصفح فقط. الميزات التي تعتمد على بيئة سطح المكتب (مثل تصدير الملفات) لن تعمل بشكل صحيح. لتجربة التطبيق الكامل على ويندوز، استخدم الأمر الموضح في القسم التالي.

### 3. التشغيل كـ تطبيق سطح مكتب (Windows/Linux) - **الطريقة الصحيحة للتجربة**

تم تجهيز المشروع بالكامل للعمل مع Electron. لتجربة النسخة الكاملة للتطبيق على ويندوز مع جميع الميزات، استخدم الأمر التالي:

```bash
# شغل هذا الأمر في الطرفية (Terminal)
npm run electron:dev
```
سيقوم هذا الأمر بتشغيل خادم التطوير لتطبيق الويب وفي نفس الوقت فتح نافذة Electron التي تعرض التطبيق بشكل متكامل.

#### بناء نسخة ويندوز قابلة للتثبيت (`.exe`)

```bash
# هذا الأمر سيقوم ببناء نسخة الويب أولاً ثم تغليفها
npm run electron:build
```
بعد انتهاء العملية، ستجد ملف التثبيت `.exe` داخل مجلد `dist_electron`.

### 4. التشغيل كـ تطبيق أندرويد

```bash
# مزامنة نسخة الويب المبنية مع مشروع الأندرويد
npx cap sync android

# فتح المشروع في Android Studio
npx cap open android
```
من داخل Android Studio، يمكنك تشغيل التطبيق على محاكي أو جهاز حقيقي.

### متطلبات المنصات المستهدفة

- **Capacitor (Android):**
  - **الكاميرا:** يتطلب التطبيق إذن الوصول إلى الكاميرا لمسح الباركود (تمت إضافته).
  - **البلوتوث:** لطباعة الإيصالات على طابعات حرارية، يتطلب التطبيق أذونات البلوتوث (تمت إضافتها في `capacitor.config.json`).

---
تم تطوير هذا المشروع لتقديم حل متكامل وموثوق لإدارة الأعمال التجارية الصغيرة والمتوسطة.